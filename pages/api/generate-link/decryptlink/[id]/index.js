// import { DecryptLink } from 'services/axios.service';

// export default async ({ query }, res) => {
//   const results = await DecryptLink(query.id)
//   if (results.responseCode === '200') {
//     res.send({url: results.data})
//   } else {
//     res.send({error: 'Failed to redirect'})
//   }
// }
